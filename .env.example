# =============================================================================
# CONFIGURACION DE VARIABLES DE ENTORNO - Sistema de Gestion de Cartas Fianzas
# =============================================================================
# 
# Este archivo contiene ejemplos de las variables de entorno necesarias.
# Copia este archivo a .env.dev (desarrollo) o .env.prod (produccion) y
# ajusta los valores segun tu entorno.
#
# DESARROLLO:   cp .env.example .env.dev
# PRODUCCION:   cp .env.example .env.prod
#

# =============================================================================
# CONFIGURACION DE POSTGRESQL (Docker)
# =============================================================================

# Nombre de la base de datos PostgreSQL
POSTGRES_DB=cartas_fianzas_db

# Usuario de PostgreSQL
POSTGRES_USER=db_admin

# Contrasena de PostgreSQL
# PRODUCCION: Usa una contrasena fuerte y segura
POSTGRES_PASSWORD=Y#4qZ!9p@rC2^mL8


# =============================================================================
# CONFIGURACION DE BASE DE DATOS (Django)
# =============================================================================

# Nombre de la base de datos (debe coincidir con POSTGRES_DB)
DB_NAME=cartas_fianzas_db

# Usuario de la base de datos (debe coincidir con POSTGRES_USER)
DB_USER=db_admin

# Contrasena de la base de datos (debe coincidir con POSTGRES_PASSWORD)
# PRODUCCION: Usa una contrasena fuerte y segura
DB_PASSWORD=Y#4qZ!9p@rC2^mL8

# Host de la base de datos
# DESARROLLO: db (nombre del servicio en docker-compose)
# PRODUCCION: db o la IP/hostname de tu servidor de BD
DB_HOST=db

# Puerto de la base de datos
DB_PORT=5432


# =============================================================================
# CONFIGURACION DE DJANGO
# =============================================================================

# Clave secreta de Django
# IMPORTANTE: En produccion, genera una clave unica y segura
# Puedes generar una con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=01!+x#-bo*-t##061=31a&@b_%-7wh(334dt&@insk4)($8c7c

# Modo debug de Django
# DESARROLLO: True
# PRODUCCION: False (NUNCA uses True en produccion)
DJANGO_DEBUG=True

# Hosts permitidos (separados por comas, sin espacios)
# DESARROLLO: localhost,127.0.0.1,backend
# PRODUCCION: tu-dominio.com,www.tu-dominio.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend


# =============================================================================
# CONFIGURACION DEL FRONTEND (React)
# =============================================================================

# URL de la API backend
# DESARROLLO: http://localhost:8000/api
# PRODUCCION: https://tu-dominio.com/api
REACT_APP_API_URL=http://localhost:8000/api


# =============================================================================
# NOTAS ADICIONALES
# =============================================================================
#
# Para desarrollo local, los valores por defecto funcionaran correctamente.
# 
# Para produccion, asegurate de cambiar:
# 1. DJANGO_SECRET_KEY - Genera una clave unica y segura
# 2. DJANGO_DEBUG - Debe ser False
# 3. DB_PASSWORD y POSTGRES_PASSWORD - Usa contrasenas fuertes
# 4. DJANGO_ALLOWED_HOSTS - Agrega tus dominios reales
# 5. REACT_APP_API_URL - Apunta a tu dominio de produccion
#
# =============================================================================

django_admin
vR6!dT9@hP3#yL2w